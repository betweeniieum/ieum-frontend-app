import { FC, PropsWithChildren } from 'react';
import { DocFooter } from './styled';

const Footer: FC<PropsWithChildren> = (props) => {
  return <DocFooter>풋터 영역입니다.</DocFooter>;
};

export default Footer;
